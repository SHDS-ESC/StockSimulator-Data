# StockSimulator-Data 프로젝트 기능 정리

## 📋 프로젝트 개요
주식 예측 머신러닝 API 서버로, S&P500 주식 데이터를 기반으로 머신러닝 모델을 활용한 주가 예측 및 포트폴리오 분석 시스템입니다.

---

## 🎯 핵심 기능

### 1. 머신러닝 기반 주식 예측 시스템
- **모델**: LightGBM Regressor (기본), RandomForest Regressor 지원
- **예측 대상**: 일일 수익률 → 가격 변환
- **예측 기간**: 1~30일 (사용자 지정 가능)
- **학습 데이터**: 사용자 지정 기간 (기본 300일)
- **특징**:
  - 슬라이딩 윈도우 방식으로 다중 윈도우 학습 및 검증
  - Recursive prediction (재귀적 예측)으로 멀티스텝 예측
  - Feature importance 추출 및 분석
  - 모델 파라미터 동적 조정 가능

### 2. 고급 피처 엔지니어링
- **기술 분석 지표** (20+ 지표):
  - RSI (상대강도지수)
  - MACD (이동평균수렴확산)
  - ATR (평균진폭)
  - 스토캐스틱 오실레이터
  - OBV (온밸런스 볼륨)
  - 볼린저 밴드
  - 이동평균선 (SMA 5, 10, 20, 50일)
  - 가격/이동평균 비율
  - 수익률 피처 (1일, 3일, 5일, 10일)
  - 변동성 지표 (10일, 20일)
  - 거래량 분석 (거래량 비율, 이동평균)
  - Lagged features (시차 피처)

- **피처 최적화**:
  - 자동 피처 선택 및 필터링
  - Feature-Target 상관관계 분석
  - 피처 중요도 기반 분석

### 3. 슬라이딩 윈도우 검증 시스템
- **다중 윈도우 학습**: 
  - 배치 크기(batch_size)와 스텝 크기(step_size) 조정 가능
  - 윈도우별 성능 검증 및 메트릭 추적
- **성능 평가 지표**:
  - 1-step 검증: MAE, RMSE, 방향 정확도
  - Multi-step 검증: MAE, RMSE, 방향 정확도, 가격 RMSE
  - 윈도우별 성능 요약 및 평균 계산
- **최종 예측 정책**: 가장 최신 윈도우의 예측 결과 사용

### 4. 투자 분석 및 리스크 평가
- **투자 지표 계산**:
  - 예상 총 수익률
  - 예상 일평균 수익률
  - 예측 변동성
  - 상승 확률
  - 예측 최고/최저 가격

- **리스크 메트릭**:
  - 연율화 변동성 (Historical & Predicted)
  - VaR (Value at Risk) 95%
  - 최대 예상 손실/수익
  - Sharpe Ratio 추정

- **투자 추천 시스템**:
  - 점수 기반 평가 (점수 범위: -5 ~ +5)
  - 신호 분석 (수익률, 상승 확률, 리스크)
  - 최종 액션 추천: BUY, SELL, HOLD
  - 신뢰도 평가: HIGH, MEDIUM, LOW

### 5. 포트폴리오 분석 시스템
- **포트폴리오 구성**:
  - 다중 포트폴리오 동시 분석
  - 티커별 가중치 설정
  - 기간별 누적 수익률 계산
  - 리밸런싱 정책 지원 (none, daily, monthly)

- **QuantStats 기반 성과 분석**:
  - 누적 수익률 (Cumulative Return)
  - CAGR (연평균 복리 수익률)
  - Sharpe Ratio
  - Sortino Ratio
  - Omega Ratio
  - 최대 낙폭 (Max Drawdown)
  - Recovery Factor
  - Ulcer Index
  - Serenity Index
  - 기간별 수익률 (MTD, 3M, 6M, YTD, 1Y, 3Y, 5Y, 10Y 등)
  - 변동성 (연율화)

### 6. RESTful API 서비스
- **FastAPI 기반**:
  - 자동 API 문서 생성 (Swagger UI, ReDoc)
  - Pydantic 기반 요청/응답 검증
  - CORS 지원
  - 전역 예외 처리 및 로깅

- **주요 엔드포인트**:
  - `POST /predict`: 주식 예측 요청
  - `GET /tickers`: 사용 가능한 티커 목록 조회
  - `GET /data/{ticker}`: 특정 티커의 주식 데이터 조회
  - `POST /portfolio/analysis`: 포트폴리오 분석
  - `GET /health`: 헬스 체크
  - `GET /scheduler/status`: 스케줄러 상태 조회

### 7. 데이터베이스 연동 및 캐싱
- **MySQL 데이터베이스**:
  - S&P500 주식 데이터 저장
  - 효율적인 데이터 로딩 (연결 풀링)
  - 티커별 데이터 캐싱 시스템
  - 멀티 티커 데이터 병합 지원

- **캐시 관리**:
  - 티커별 데이터 캐싱
  - 캐시 정보 조회 API
  - 수동 캐시 클리어 기능

### 8. 스케줄러 서비스
- **백그라운드 작업**:
  - 일일 캐시 새로고침 (월-금, EST 16:30)
  - 주간 정리 작업 (일요일, EST 02:00)
  - 시장 데이터 업데이트 (월-금, EST 08:00)
  - 작업 강제 실행 기능

### 9. 차트 생성 및 시각화
- **예측 차트 생성**:
  - 전체 데이터 차트 (다운샘플링 적용)
  - 최근 50일 요약 차트
  - Base64 인코딩으로 API 응답에 포함
  - 파일 저장 기능 (output 폴더)

- **차트 메타데이터**:
  - 모델 정보
  - 성능 메트릭
  - 파라미터 정보
  - 데이터셋 정보

### 10. 로깅 및 모니터링
- **구조화된 로깅**:
  - 요청/응답 로깅
  - 예외 추적 및 상세 로그
  - 파일 및 콘솔 출력
  - 로그 레벨 설정 가능

---

## 🏗️ 아키텍처 특징

### 계층화된 구조
- **Controller Layer**: API 엔드포인트 관리
- **Service Layer**: 비즈니스 로직 처리
- **Model Layer**: 데이터 전송 객체 (DTO)
- **Database Layer**: 데이터 접근 및 캐싱

### 확장 가능한 설계
- 모델 교체 가능 (LightGBM, RandomForest 등)
- 피처 엔지니어링 모듈화
- 서비스별 독립적 개발 가능
- 의존성 주입 패턴 사용

---

## 🔧 기술 스택

### 백엔드
- **FastAPI**: 웹 프레임워크
- **SQLAlchemy**: ORM 및 데이터베이스 연결
- **MySQL**: 데이터베이스

### 머신러닝
- **LightGBM**: 그래디언트 부스팅 모델
- **scikit-learn**: 전처리 및 평가 도구
- **TA-Lib**: 기술 분석 지표 계산
- **NumPy, Pandas**: 데이터 처리

### 기타
- **APScheduler**: 백그라운드 작업 스케줄링
- **Matplotlib**: 차트 생성
- **Pydantic**: 데이터 검증

---

## 📊 데이터 흐름

1. **데이터 로드**: MySQL에서 티커별 주식 데이터 로드
2. **피처 생성**: 기술 분석 지표 및 엔지니어링 피처 계산
3. **모델 학습**: 슬라이딩 윈도우 방식으로 다중 학습
4. **예측 실행**: Recursive prediction으로 멀티스텝 예측
5. **성능 검증**: 윈도우별 성능 메트릭 계산
6. **투자 분석**: 예측 결과 기반 투자 추천 생성
7. **시각화**: 차트 생성 및 메타데이터 포함
8. **API 응답**: 구조화된 JSON 응답 반환

---

## 🎓 JD 연관성 및 확장 가능성

### 현재 구현된 기능
1. **모델 평가 및 피드백 루프**:
   - 슬라이딩 윈도우 검증으로 모델 성능 지속 평가
   - 윈도우별 메트릭 추적 및 평균 계산
   - Feature importance 분석으로 모델 해석성 확보

2. **피처 엔지니어링**:
   - 20+ 기술 분석 지표 자동 계산
   - Feature-Target 상관관계 분석
   - 동적 피처 선택 및 최적화

3. **시스템 구조**:
   - 확장 가능한 서비스 기반 아키텍처
   - 모듈화된 설계로 Agent 시스템으로 확장 가능

### 확장 가능한 영역 (JD 요구사항 연계)
1. **Multi Agent System**:
   - 현재 단일 예측 Agent → 다중 Agent로 확장 가능
   - 각 Agent가 다른 모델/전략 담당
   - Agent 간 협업 및 결과 통합

2. **LLM 통합**:
   - 예측 결과 해석 및 리포트 생성
   - 자연어 기반 쿼리 처리
   - 투자 추천 설명 생성

3. **Graph RAG**:
   - 주식 간 관계 그래프 구축
   - 섹터/산업별 연관성 분석
   - 그래프 기반 컨텍스트 활용

4. **Text-to-SQL**:
   - 자연어로 데이터베이스 쿼리 생성
   - 사용자 친화적 데이터 조회
   - 동적 리포트 생성

5. **Context/Prompt Engineering**:
   - 모델 파라미터 최적화
   - 피처 선택 전략 개선
   - 하이퍼파라미터 튜닝 자동화

---

## 📈 성능 및 최적화

- **메모리 효율성**: 
  - 티커별 데이터 캐싱
  - 필요 시에만 데이터 로드
  - 차트 다운샘플링

- **처리 속도**:
  - 연결 풀링으로 DB 접근 최적화
  - 배치 처리 지원
  - 병렬 처리 가능 구조

- **확장성**:
  - 서비스별 독립적 스케일링 가능
  - 마이크로서비스 아키텍처로 전환 용이

---

## 🔍 주요 파일 구조

```
py/
├── main.py                    # FastAPI 메인 애플리케이션
├── config/
│   └── settings.py            # 설정 관리
├── controllers/               # API 컨트롤러
│   ├── stock_controller.py
│   ├── health_controller.py
│   └── scheduler_controller.py
├── services/                  # 비즈니스 로직
│   ├── stock_service.py       # 주식 예측 메인 서비스
│   ├── prediction_service.py  # ML 모델 서비스
│   ├── analysis_service.py    # 투자 분석 서비스
│   ├── database_service.py   # 데이터베이스 서비스
│   ├── chart_service.py      # 차트 생성 서비스
│   └── scheduler_service.py  # 스케줄러 서비스
└── models/
    └── dto.py                 # 데이터 전송 객체
```

---

## 💡 프로젝트의 강점

1. **실전 적용 가능한 구조**: 프로덕션 레벨의 코드 구조와 에러 처리
2. **확장 가능한 아키텍처**: Agent 시스템으로 확장 용이
3. **포괄적인 분석**: 예측뿐만 아니라 투자 분석 및 리스크 평가 포함
4. **성능 검증**: 슬라이딩 윈도우로 지속적인 모델 평가
5. **사용자 친화적 API**: 자동 문서화 및 명확한 응답 구조
6. **데이터 효율성**: 캐싱 및 최적화된 데이터 로딩
